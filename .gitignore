/target
/docs/.next
/docs/node_modules
/docs/out
/src/static

# In case I accidentally commit mine...
cup.json

# The live deployment compose file is host-specific config, not source: it sits in
# the deploy checkout on the host but is deliberately untracked, so `git pull` and
# branch switches can never clobber it. Start from deploy/docker-compose.example.yml.
/docker-compose.yaml
/docker-compose.yml

# Profiling results don't need to be present in the repo
profile.json